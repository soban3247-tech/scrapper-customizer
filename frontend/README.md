# Frontend

Next.js browser interface for CV upload, profile review, scraper configuration,
and normalized job results. It calls the FastAPI backend through
`NEXT_PUBLIC_API_URL`.

```powershell
npm install
npm run dev
```

The default backend URL is `http://localhost:8000`.
